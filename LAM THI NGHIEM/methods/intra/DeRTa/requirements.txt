# Shared by every LOCAL (in/intra) method: core.local_client + core.runner
torch
transformers
accelerate
peft
pandas
requests

# ----- DeRTa: inference needs only the block above (base + LoRA adapter) -----
# Upstream pins (repo/requirement.txt): transformers==4.40.0 accelerate==0.27.2
#   datasets==2.10.0 torch==2.2.0+cu118 + DeepSpeed from git
datasets            # train only
evaluate            # train only
# deepspeed         # train only - NOT needed by train_smoke.py (single GPU, no deepspeed)
